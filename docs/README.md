# Documentation

Feature and reference docs for the **Document Auto-Approval System**. Start at the
[project README](../README.md) for setup and the overall pipeline; use this index to jump
into individual features.

## Contents

| Doc | What it covers |
| --- | --- |
| [Digital Signing (PAdES)](digital-signing.md) | Outbound signing of an **approved** document's PDF with a real X.509 certificate + signature validation — the `pyhanko`/`mock` providers, the sign/validate endpoints, the `SignaturePanel` UI, the `SIGNING_*` config, the demo custody model + security notes, and the PAdES level roadmap. |

---

← Back to the [project README](../README.md).
